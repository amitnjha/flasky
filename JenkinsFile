pipeline {
    stages {
        stage('build') {
            steps {
                sh 'pip3 install -r requirements.txt'
            }
        }
        stage('test1') {
            steps {
                sh 'flask test'
            }
            post {
                always {junit 'test-reports/*.xml'}
            }
        }
    }
}